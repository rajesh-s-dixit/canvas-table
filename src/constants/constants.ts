export const DEFAULT_COLUMN_WIDTH = 100; // Default width for columns if not specified
export const DEFAULT_ROW_HEIGHT = 30; // Default height for each row in the table
export const DEFAULT_HEIGHT = 500; // Default height for the table
export const DEFAULT_WIDTH = 800; // Default width for the table